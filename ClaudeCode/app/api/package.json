{
  "name": "recipe-app-api",
  "version": "1.0.0",
  "description": "Recipe Management API - Lambda Functions",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../lambda.zip . && cd .. && zip lambda.zip package.json && zip -r lambda.zip node_modules",
    "clean": "rm -rf dist lambda.zip"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.985.0",
    "jsonwebtoken": "^9.0.0",
    "jwks-rsa": "^3.1.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^20.17.0",
    "typescript": "^5.7.0"
  }
}
